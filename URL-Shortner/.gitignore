# Generated by seed_data.py - regenerate rather than commit
loadtest/seeded_codes.json

# Load test output - can be large (k6's raw per-request CSVs especially);
# summarize into docs/06-results.md instead of committing raw runs
loadtest/results/

# Python
fastapi-service/.venv/
**/__pycache__/
*.pyc

# Go build artifacts (vendor/ IS committed on purpose - see go-service/Dockerfile,
# it's what lets the Docker build work without network access)
go-service/urlshortener
